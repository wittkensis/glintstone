fastapi>=0.115
uvicorn[standard]>=0.30
jinja2>=3.1
python-dotenv>=1.0
psycopg[binary]>=3.2
psycopg-pool>=3.2
pydantic-settings>=2.0
httpx>=0.27
itsdangerous>=2.2
resend>=2.0
Pillow>=10.0
python-multipart>=0.0.9
boto3>=1.34

# Agentic surface (see .claude/skills/gs-expert-agentic/)
anthropic>=0.75
voyageai>=0.3
mcp>=1.27

# Docs build (ops/build_docs.py — runs on the CI runner during deploy)
markdown>=3.5
PyYAML>=6.0

# Akkademia sign-recognition model (ingestion/connectors/akkademia_signs.py, #535).
# OPTIONAL + NOT INSTALLED in the default runtime: the `akkadian` package pins
# Python 3.7.x + torch + allennlp 0.8.5 and will NOT install under Python 3.13.
# The connector imports it lazily and no-ops with a clear log line when absent,
# so the rest of ingestion is unaffected. Install in a dedicated 3.7 venv to run:
#   pip install torch torchvision && pip install akkadian
# akkadian==1.0.12   # gaigutherz/Akkademia — enable only in a Python 3.7 env
