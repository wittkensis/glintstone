APP_ENV=local
APP_DEBUG=true

DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=glintstone
DB_USER=glintstone
DB_PASSWORD=glintstone

# Local dev ports (uvicorn backends — nginx proxies .test domains to these)
API_PORT=8001
WEB_PORT=8002

# URLs (used for CORS, links between sites)
API_URL=http://api.glintstone.test
WEB_URL=http://app.glintstone.test
MARKETING_URL=http://glintstone.test

# Image storage path
IMAGE_PATH=./app-v0.1/database/images

# Deploy target (VPS IP or hostname)
# DEPLOY_HOST=your-vps-ip
